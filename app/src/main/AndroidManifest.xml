<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dk.nicklasslagbrand.forecast">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name="dk.nicklasslagbrand.forecast.presentation.places.MainActivity"></activity>
        <activity android:name="dk.nicklasslagbrand.forecast.presentation.places.ui.details.PlaceDetailsActivity" />
        <activity android:name="dk.nicklasslagbrand.forecast.presentation.places.ui.list.PlacesActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>